/**
 * 
 */
/*
var webService = "http://checkip.amazonaws.com";

$.ajax({
    url: webService,
    data: "", //ur data to be sent to server
    contentType: "application/json; charset=utf-8", 
    type: "GET",
    success: function (data) {
       alert(data);
    },
    error: function (x, y, z) {
       alert(x.responseText +"  " +x.status);
    }
});

function func() 
{
	var data="hello";
	$.get(webService, function (response) {
		data = response;
	}).error(function(){
		alert("Sorry could not proceed");
	});

	return data;
}

function MyIP() {
		
	document.getElementById("formMain:ip-address").value = func();
}
*/